# Core ML libraries
torch>=2.0.0
torchvision>=0.15.0

# Data processing and augmentation
numpy>=1.23.0
Pillow>=9.0.0
albumentations>=1.3.0

# Utilities
tqdm>=4.64.0

# Notes:
# - Install a matching CUDA-enabled PyTorch wheel if you need GPU support.
#   See https://pytorch.org/get-started/locally/ for the correct install command.
# - If you want strict reproducibility, replace ">=" with pinned versions (e.g. "==2.0.1").
